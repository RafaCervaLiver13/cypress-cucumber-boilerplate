{
  "name": "cypress",
  "version": "3.0.4",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress open",
    "cypress:mobile": "cypress run --browser electron --headed --env",
    "cypress:desktop": "cypress run --browser electron --headed --config-file cypress.config-desktop.js --env",
    "cypress:open:desktop": "cypress open --config-file cypress.config-desktop.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.3",
    "@deepakvishwakarma/cucumber-json-formatter": "^0.0.3",
    "cypress": "^13.15.0",
    "cypress-on-fix": "^1.0.3",
    "cypress-split": "^1.24.0",
    "esbuild": "^0.24.0",
    "multiple-cucumber-html-reporter": "^3.8.0"
  },
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^21.0.2"
  }
}
